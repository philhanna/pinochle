{
  "name": "pinochle-frontend",
  "version": "0.3.0",
  "private": true,
  "description": "Browser client for Pinochle: TypeScript, no framework, no bundler (ARC-8)",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "tsc && node --test \"test/*.test.js\""
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
